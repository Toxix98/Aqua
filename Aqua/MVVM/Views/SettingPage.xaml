<Page x:Class="Aqua.Pages.SettingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Aqua.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="650" d:DesignWidth="1010"
      Title="SettingPage">

    <Border Background="WhiteSmoke">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <Button Style="{StaticResource MenuButtonStorPage}" Width="207" Margin="0 10 0 0" Background="#1067aa" Content="ویرایش حساب کاربری" FontSize="11"/>
                <Button Style="{StaticResource MenuButtonStorPage}" Width="207" Margin="0 40 0 0" Background="#1067aa" Content="ویرایش حساب کاربری" FontSize="11"/>
                <Button Style="{StaticResource MenuButtonStorPage}" Width="207" Margin="0 40 0 0" Background="#1067aa" Content="ویرایش حساب کاربری" FontSize="11"/>
                <Button Style="{StaticResource MenuButtonStorPage}" Width="207" Margin="0 40 0 0" Background="#1067aa" Content="ویرایش حساب کاربری" FontSize="11"/>
            </StackPanel>
            
        </Grid>
    </Border>
</Page>
